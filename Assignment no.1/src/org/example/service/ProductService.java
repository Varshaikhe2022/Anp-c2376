package org.example.service;
import org.example.model.Product;
import org.example.model.product;
public class ProductService {
	public void increasePrice(Product p)
	
	{
		double newprice;
		System.out.println(p.getPrice()); //print the price in the product class
		newprice.getPrice()+100 //increase the price in 100 to the original price
		
		System.out.println("increase price is"+newprice); //print the increase price
		
	}

}
